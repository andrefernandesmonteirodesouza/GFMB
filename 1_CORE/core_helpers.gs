const CONFIDENCE_COLORS = {
    HIGH: { threshold: 0.95, bg: "#e8f5f9", font: "#1b5e20" }, 
    GOOD: { threshold: 0.80, bg: "#e3f2fd", font: "#0d47a1" },
    MID: { threshold: 0.65, bg: "#fff3e0", font: "#e65100" },
    LOW: { threshold: 0.00, bg: "#fffde7", font: "#f57f17" }
};